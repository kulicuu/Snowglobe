


const fragSrc = `



void main() {

  // vec2 uv = gl_FragCoord.xy / viewPortSize;
  // float y2 = pow(gl_FragCoord.y, 2);



  gl_FragColor = vec4(0.0,1.0,0.7,1.0);
}


`;


export default fragSrc;
